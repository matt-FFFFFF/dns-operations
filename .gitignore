# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
tfplan
tfplan-*
*.tfplan

# Generated: the document the policy is evaluated against. Rebuilt by
# `make policy`; never reviewed, never committed.
/build/

# Zone exports pulled from Cloudflare while adopting a zone.
*.zone.txt

# Python and lint caches
__pycache__/
.ruff_cache/
